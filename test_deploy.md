# Тестирование развернутого бота

После успешного деплоя на Render важно убедиться, что бот работает корректно. Вот как можно это проверить:

1.  **Проверка логов Render**:
    *   Перейдите в панель управления Render.
    *   Найдите свой сервис.
    *   Перейдите на вкладку "Manual Deploy" или "Automatic Deploys".
    *   Проверьте последние логи. В них не должно быть ошибок, связанных с запуском бота, подключением к API Telegram или TikTok.
    *   Вы должны увидеть сообщения вроде `🤖 Запуск бота...` и `✅ Бот готов!`.

2.  **Проверка функциональности бота**:
    *   Откройте Telegram и найдите вашего бота.
    *   Отправьте команду `/start`. Бот должен ответить приветственным сообщением и клавиатурой.
    *   Отправьте команду `/help`. Бот должен ответить справкой по командам.
    *   Убедитесь, что бот публикует видео в канал с заданным интервалом. Это может занять некоторое время в зависимости от настроенного интервала.

3.  **Проверка переменных окружения**:
    *   Убедитесь, что все переменные окружения, такие как `BOT_TOKEN`, `CHANNEL_ID`, `ADMIN_ID`, установлены правильно. Ошибки в этих переменных могут привести к ошибкам при запуске или работе бота.

4.  **Проверка сессии TikTok**:
    *   На Render сессия TikTok (`tiktok_session.json`) не сохраняется между перезапусками. Это означает, что при каждом запуске бота ему нужно будет заново проходить процесс аутентификации, если сессия была потеряна. Это может повлиять на стабильность работы. В будущем можно рассмотреть использование постоянного хранилища или альтернативных методов аутентификации.

Если все работает корректно, значит, бот успешно развернут на Render.